node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json
rules.json

# graphify AST cache (regenerated by `graphify update .`)
graphify-out/cache/
# graphify pipeline sidecars: temp files of a run, plus .graphify_root, which holds
# an absolute path from whichever machine built the graph. All regenerated; none belong
# in the repo.
graphify-out/.graphify_*
